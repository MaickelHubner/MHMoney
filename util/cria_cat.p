DISABLE TRIGGERS FOR LOAD OF categoria.
DISABLE TRIGGERS FOR LOAD OF sub-cat.

CREATE categoria.
ASSIGN categoria.cod-categoria = 1
       categoria.ds-categoria = "Sal rios e Ordenados"
       categoria.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 1
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Pagamento"
       sub-cat.id-tipo = 0.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 1
       sub-cat.cd-sub = 2
       sub-cat.ds-sub = "F‚rias"
       sub-cat.id-tipo = 0.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 1
       sub-cat.cd-sub = 3
       sub-cat.ds-sub = "13§ Sal rio"
       sub-cat.id-tipo = 0.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 1
       sub-cat.cd-sub = 4
       sub-cat.ds-sub = "PPR"
       sub-cat.id-tipo = 0.
CREATE categoria.
ASSIGN categoria.cod-categoria = 2
       categoria.ds-categoria = "Rendimentos de Investimento"
       categoria.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 2
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Juros"
       sub-cat.id-tipo = 0.
CREATE categoria.
ASSIGN categoria.cod-categoria = 3
       categoria.ds-categoria = "Outros Rendimentos"
       categoria.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 3
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Presentes recebidos"
       sub-cat.id-tipo = 0.
CREATE categoria.
ASSIGN categoria.cod-categoria = 4
       categoria.ds-categoria = "Alimenta‡Æo"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 4
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Supermercado"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 4
       sub-cat.cd-sub = 2
       sub-cat.ds-sub = "Restaurante"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 4
       sub-cat.cd-sub = 3
       sub-cat.ds-sub = "Pizzaria"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 4
       sub-cat.cd-sub = 4
       sub-cat.ds-sub = "Almo‡o Di rio"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 4
       sub-cat.cd-sub = 5
       sub-cat.ds-sub = "Lanchonete"
       sub-cat.id-tipo = 1.
CREATE categoria.
ASSIGN categoria.cod-categoria = 5
       categoria.ds-categoria = "Autom¢vel"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 5
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Combust¡vel"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 5
       sub-cat.cd-sub = 2
       sub-cat.ds-sub = "Manuten‡Æo"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 5
       sub-cat.cd-sub = 3
       sub-cat.ds-sub = "Estacionamento"
       sub-cat.id-tipo = 2.
CREATE categoria.
ASSIGN categoria.cod-categoria = 6
       categoria.ds-categoria = "Educa‡Æo"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 6
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Mensalidade"
       sub-cat.id-tipo = 2.
CREATE categoria.
ASSIGN categoria.cod-categoria = 7
       categoria.ds-categoria = "Vestu rio"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 7
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Artigos & Utilit rios"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 7
       sub-cat.cd-sub = 2
       sub-cat.ds-sub = "Inverno"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 7
       sub-cat.cd-sub = 3
       sub-cat.ds-sub = "VerÆo"
       sub-cat.id-tipo = 1.
CREATE categoria.
ASSIGN categoria.cod-categoria = 8
       categoria.ds-categoria = "Despesas Dom‚sticas"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 8
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Aluguel"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 8
       sub-cat.cd-sub = 2
       sub-cat.ds-sub = "Condom¡nio"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 8
       sub-cat.cd-sub = 3
       sub-cat.ds-sub = "Luz"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 8
       sub-cat.cd-sub = 4
       sub-cat.ds-sub = "µgua"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 8
       sub-cat.cd-sub = 5
       sub-cat.ds-sub = "Telefone"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 8
       sub-cat.cd-sub = 6
       sub-cat.ds-sub = "G s"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 8
       sub-cat.cd-sub = 7
       sub-cat.ds-sub = "Empregada & Diarista"
       sub-cat.id-tipo = 2.
CREATE categoria.
ASSIGN categoria.cod-categoria = 9
       categoria.ds-categoria = "Impostos & Tarifas P£blicas"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 9
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "CPMF"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 9
       sub-cat.cd-sub = 2
       sub-cat.ds-sub = "IRRF"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 9
       sub-cat.cd-sub = 3
       sub-cat.ds-sub = "INSS"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 9
       sub-cat.cd-sub = 4
       sub-cat.ds-sub = "IPTU"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 9
       sub-cat.cd-sub = 5
       sub-cat.ds-sub = "Taxa de Coleta de Lixo"
       sub-cat.id-tipo = 2.
CREATE categoria.
ASSIGN categoria.cod-categoria = 10
       categoria.ds-categoria = "Lazer"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 10
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Cinema"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 10
       sub-cat.cd-sub = 2
       sub-cat.ds-sub = "Eventos"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 10
       sub-cat.cd-sub = 3
       sub-cat.ds-sub = "Loca‡Æo de V¡deo"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 10
       sub-cat.cd-sub = 4
       sub-cat.ds-sub = "Shows"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 10
       sub-cat.cd-sub = 5
       sub-cat.ds-sub = "TV por Assinatura"
       sub-cat.id-tipo = 1.
CREATE categoria.
ASSIGN categoria.cod-categoria = 11
       categoria.ds-categoria = "Higiene Pessoal"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 11
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Cabelereiro"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 11
       sub-cat.cd-sub = 2
       sub-cat.ds-sub = "Perfumes & Desodorantes"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 11
       sub-cat.cd-sub = 3
       sub-cat.ds-sub = "Artigos de Banho"
       sub-cat.id-tipo = 1.
CREATE categoria.
ASSIGN categoria.cod-categoria = 12
       categoria.ds-categoria = "Sa£de"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 12
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Plano de Sa£de"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 12
       sub-cat.cd-sub = 2
       sub-cat.ds-sub = "Consulta M‚dica"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 12
       sub-cat.cd-sub = 3
       sub-cat.ds-sub = "Plano Odontol¢gico"
       sub-cat.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 12
       sub-cat.cd-sub = 4
       sub-cat.ds-sub = "Rem‚dios"
       sub-cat.id-tipo = 1.
CREATE categoria.
ASSIGN categoria.cod-categoria = 13
       categoria.ds-categoria = "Despesas Financeiras"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 13
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Taxa de Manuten‡Æo de Conta"
       sub-cat.id-tipo = 2.
CREATE categoria.
ASSIGN categoria.cod-categoria = 14
       categoria.ds-categoria = "F‚rias"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 14
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Lazer"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 14
       sub-cat.cd-sub = 2
       sub-cat.ds-sub = "Alimenta‡Æo"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 14
       sub-cat.cd-sub = 3
       sub-cat.ds-sub = "Viagens & Pacotes Tur¡sticos"
       sub-cat.id-tipo = 1.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 14
       sub-cat.cd-sub = 4
       sub-cat.ds-sub = "Hotel"
       sub-cat.id-tipo = 1.
CREATE categoria.
ASSIGN categoria.cod-categoria = 15
       categoria.ds-categoria = "Transporte"
       categoria.id-tipo = 2.
CREATE sub-cat.
ASSIGN sub-cat.cod-categoria = 15
       sub-cat.cd-sub = 1
       sub-cat.ds-sub = "Passe Di rio"
       sub-cat.id-tipo = 2.
